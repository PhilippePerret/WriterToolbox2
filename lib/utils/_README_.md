Dossier des modules utiles.

> Note : ce dossier n'est pas chargé par défaut (et ne devra jamais l'être). Il faut requérir les éléments indépendamment par :

```

require './lib/utils/<affixe du module>'

```
