<%
=begin

  Pour voir le profil de l'utilisateur

=end
%>
<%= header(:small) %>
<%= right_margin %>
<%=
  partial(
    case
    when user.identified?
      case
      when u.id == user.id then 'profil-perso'
      else 'profil-complet'
      end
    else 
      case 
      when site.route.objet_id == nil then 'profil-none'
      else 'profil-public'
      end
    end
 )
%>

<%= footer(:small) %>
