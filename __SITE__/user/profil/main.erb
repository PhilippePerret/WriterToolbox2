<%
=begin

  Pour voir le profil de l'utilisateur

=end
%>
<%= header(:small) %>
<%= right_margin %>
<%=
  partial(
    case
    when user.identified?
      case
      when u.id == user.id then 'profil-perso'
      else 'profil-complet'
      end
    else 'profil-public'
    end
 )
%>

<%= footer(:small) %>
